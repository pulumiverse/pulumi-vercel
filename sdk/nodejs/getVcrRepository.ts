// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides information about an existing Vercel Container Registry (VCR) Repository.
 *
 * A VCR Repository belongs to a Vercel Project and stores container images that can be
 * used by Vercel Functions and Vercel Sandbox.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = vercel.getVcrRepository({
 *     projectId: "prj_xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
 *     name: "my-repository",
 * });
 * ```
 */
export function getVcrRepository(args: GetVcrRepositoryArgs, opts?: pulumi.InvokeOptions): Promise<GetVcrRepositoryResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vercel:index/getVcrRepository:getVcrRepository", {
        "name": args.name,
        "projectId": args.projectId,
        "teamId": args.teamId,
    }, opts);
}

/**
 * A collection of arguments for invoking getVcrRepository.
 */
export interface GetVcrRepositoryArgs {
    /**
     * The name of the repository.
     */
    name: string;
    /**
     * The ID of the existing Vercel Project the repository belongs to.
     */
    projectId: string;
    /**
     * The ID of the team the repository exists under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: string;
}

/**
 * A collection of values returned by getVcrRepository.
 */
export interface GetVcrRepositoryResult {
    /**
     * The ID of the VCR Repository.
     */
    readonly id: string;
    /**
     * The name of the repository.
     */
    readonly name: string;
    /**
     * The ID of the existing Vercel Project the repository belongs to.
     */
    readonly projectId: string;
    /**
     * The ID of the team the repository exists under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    readonly teamId: string;
    /**
     * The URL of the repository, composed of the owner slug, the project slug and the repository name (e.g. `vcr.vercel.com/team-slug/project-slug/repository-name`). Use it to push and pull images with Docker-compatible tooling.
     */
    readonly url: string;
}
/**
 * Provides information about an existing Vercel Container Registry (VCR) Repository.
 *
 * A VCR Repository belongs to a Vercel Project and stores container images that can be
 * used by Vercel Functions and Vercel Sandbox.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = vercel.getVcrRepository({
 *     projectId: "prj_xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
 *     name: "my-repository",
 * });
 * ```
 */
export function getVcrRepositoryOutput(args: GetVcrRepositoryOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetVcrRepositoryResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vercel:index/getVcrRepository:getVcrRepository", {
        "name": args.name,
        "projectId": args.projectId,
        "teamId": args.teamId,
    }, opts);
}

/**
 * A collection of arguments for invoking getVcrRepository.
 */
export interface GetVcrRepositoryOutputArgs {
    /**
     * The name of the repository.
     */
    name: pulumi.Input<string>;
    /**
     * The ID of the existing Vercel Project the repository belongs to.
     */
    projectId: pulumi.Input<string>;
    /**
     * The ID of the team the repository exists under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}
