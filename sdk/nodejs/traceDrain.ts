// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a configurable Trace Drain resource.
 *
 * Trace Drains forward OpenTelemetry trace data from your deployments to an OTLP/HTTP compatible endpoint.
 *
 * > Only Pro and Enterprise teams can create configurable Trace Drains.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = new vercel.TraceDrain("example", {
 *     name: "example-trace-drain",
 *     deliveryFormat: "json",
 *     endpoint: "https://example.com/v1/traces",
 *     projectIds: [exampleVercelProject.id],
 *     samplingRules: [{
 *         rate: 0.8,
 *         environment: "production",
 *         requestPath: "/api",
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * If importing into a personal account, or with a default team configured in
 * the provider, simply use the trace_drain_id.
 *
 * ```sh
 * $ pulumi import vercel:index/traceDrain:TraceDrain example drn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
 * ```
 *
 * Alternatively, you can import via team_id/trace_drain_id.
 *
 * ```sh
 * $ pulumi import vercel:index/traceDrain:TraceDrain example team_xxxxxxxxxxxxxxxxxxxxxxxx/drn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
 * ```
 */
export class TraceDrain extends pulumi.CustomResource {
    /**
     * Get an existing TraceDrain resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: TraceDrainState, opts?: pulumi.CustomResourceOptions): TraceDrain {
        return new TraceDrain(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vercel:index/traceDrain:TraceDrain';

    /**
     * Returns true if the given object is an instance of TraceDrain.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is TraceDrain {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === TraceDrain.__pulumiType;
    }

    /**
     * The OTLP/HTTP format trace data should be delivered in. Can be `json` or `proto`.
     */
    declare public readonly deliveryFormat: pulumi.Output<string>;
    /**
     * The OTLP/HTTP traces endpoint. This should be the full traces endpoint URL, commonly ending in `/v1/traces`.
     */
    declare public readonly endpoint: pulumi.Output<string>;
    /**
     * Custom headers to include in requests to the trace drain endpoint.
     */
    declare public readonly headers: pulumi.Output<{[key: string]: string} | undefined>;
    /**
     * A human-readable name for the trace drain.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * A list of project IDs that the trace drain should be associated with. If omitted, traces will be sent for all projects.
     */
    declare public readonly projectIds: pulumi.Output<string[] | undefined>;
    /**
     * Ordered sampling rules for traces sent to this drain. If omitted, all traces are sent.
     */
    declare public readonly samplingRules: pulumi.Output<outputs.TraceDrainSamplingRule[] | undefined>;
    /**
     * A custom secret used to sign trace drain events. If omitted, Vercel generates one.
     */
    declare public readonly secret: pulumi.Output<string>;
    /**
     * The ID of the team the Trace Drain should exist under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    declare public readonly teamId: pulumi.Output<string>;

    /**
     * Create a TraceDrain resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: TraceDrainArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: TraceDrainArgs | TraceDrainState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as TraceDrainState | undefined;
            resourceInputs["deliveryFormat"] = state?.deliveryFormat;
            resourceInputs["endpoint"] = state?.endpoint;
            resourceInputs["headers"] = state?.headers;
            resourceInputs["name"] = state?.name;
            resourceInputs["projectIds"] = state?.projectIds;
            resourceInputs["samplingRules"] = state?.samplingRules;
            resourceInputs["secret"] = state?.secret;
            resourceInputs["teamId"] = state?.teamId;
        } else {
            const args = argsOrState as TraceDrainArgs | undefined;
            if (args?.deliveryFormat === undefined && !opts.urn) {
                throw new Error("Missing required property 'deliveryFormat'");
            }
            if (args?.endpoint === undefined && !opts.urn) {
                throw new Error("Missing required property 'endpoint'");
            }
            resourceInputs["deliveryFormat"] = args?.deliveryFormat;
            resourceInputs["endpoint"] = args?.endpoint;
            resourceInputs["headers"] = args?.headers ? pulumi.secret(args.headers) : undefined;
            resourceInputs["name"] = args?.name;
            resourceInputs["projectIds"] = args?.projectIds;
            resourceInputs["samplingRules"] = args?.samplingRules;
            resourceInputs["secret"] = args?.secret ? pulumi.secret(args.secret) : undefined;
            resourceInputs["teamId"] = args?.teamId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        const secretOpts = { additionalSecretOutputs: ["headers", "secret"] };
        opts = pulumi.mergeOptions(opts, secretOpts);
        super(TraceDrain.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering TraceDrain resources.
 */
export interface TraceDrainState {
    /**
     * The OTLP/HTTP format trace data should be delivered in. Can be `json` or `proto`.
     */
    deliveryFormat?: pulumi.Input<string>;
    /**
     * The OTLP/HTTP traces endpoint. This should be the full traces endpoint URL, commonly ending in `/v1/traces`.
     */
    endpoint?: pulumi.Input<string>;
    /**
     * Custom headers to include in requests to the trace drain endpoint.
     */
    headers?: pulumi.Input<{[key: string]: pulumi.Input<string>}>;
    /**
     * A human-readable name for the trace drain.
     */
    name?: pulumi.Input<string>;
    /**
     * A list of project IDs that the trace drain should be associated with. If omitted, traces will be sent for all projects.
     */
    projectIds?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Ordered sampling rules for traces sent to this drain. If omitted, all traces are sent.
     */
    samplingRules?: pulumi.Input<pulumi.Input<inputs.TraceDrainSamplingRule>[]>;
    /**
     * A custom secret used to sign trace drain events. If omitted, Vercel generates one.
     */
    secret?: pulumi.Input<string>;
    /**
     * The ID of the team the Trace Drain should exist under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a TraceDrain resource.
 */
export interface TraceDrainArgs {
    /**
     * The OTLP/HTTP format trace data should be delivered in. Can be `json` or `proto`.
     */
    deliveryFormat: pulumi.Input<string>;
    /**
     * The OTLP/HTTP traces endpoint. This should be the full traces endpoint URL, commonly ending in `/v1/traces`.
     */
    endpoint: pulumi.Input<string>;
    /**
     * Custom headers to include in requests to the trace drain endpoint.
     */
    headers?: pulumi.Input<{[key: string]: pulumi.Input<string>}>;
    /**
     * A human-readable name for the trace drain.
     */
    name?: pulumi.Input<string>;
    /**
     * A list of project IDs that the trace drain should be associated with. If omitted, traces will be sent for all projects.
     */
    projectIds?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Ordered sampling rules for traces sent to this drain. If omitted, all traces are sent.
     */
    samplingRules?: pulumi.Input<pulumi.Input<inputs.TraceDrainSamplingRule>[]>;
    /**
     * A custom secret used to sign trace drain events. If omitted, Vercel generates one.
     */
    secret?: pulumi.Input<string>;
    /**
     * The ID of the team the Trace Drain should exist under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}
