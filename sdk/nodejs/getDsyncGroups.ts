// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides information about DSync groups for a team.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = vercel.getDsyncGroups({
 *     teamId: "team_xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
 * });
 * const contractor = new vercel.AccessGroup("contractor", {
 *     teamId: "team_xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
 *     name: "contractor",
 *     description: "Access group for contractors",
 * });
 * const exampleTeamConfig = new vercel.TeamConfig("example", {
 *     id: "team_xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
 *     saml: {
 *         enforced: true,
 *         roles: {
 *             [std.index.lookup({
 *                 map: exampleVercelDsyncGroups.map,
 *                 key: "admin",
 *             }).result]: {
 *                 role: "OWNER",
 *             },
 *             [std.index.lookup({
 *                 map: exampleVercelDsyncGroups.map,
 *                 key: "finance",
 *             }).result]: {
 *                 role: "BILLING",
 *             },
 *             [std.index.lookup({
 *                 map: exampleVercelDsyncGroups.map,
 *                 key: "contractor",
 *             }).result]: {
 *                 role: "CONTRIBUTOR",
 *                 accessGroupId: contractor.id,
 *             },
 *         },
 *     },
 * });
 * ```
 */
export function getDsyncGroups(args?: GetDsyncGroupsArgs, opts?: pulumi.InvokeOptions): Promise<GetDsyncGroupsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vercel:index/getDsyncGroups:getDsyncGroups", {
        "teamId": args.teamId,
    }, opts);
}

/**
 * A collection of arguments for invoking getDsyncGroups.
 */
export interface GetDsyncGroupsArgs {
    /**
     * The ID of the team the Dsync Groups are associated to. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: string;
}

/**
 * A collection of values returned by getDsyncGroups.
 */
export interface GetDsyncGroupsResult {
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * A list of DSync groups for the team.
     */
    readonly lists: outputs.GetDsyncGroupsList[];
    /**
     * A map of Identity Provider group names to their Vercel IDs. This can be used to look up the ID of a group by its name using the lookup function.
     */
    readonly map: {[key: string]: string};
    /**
     * The ID of the team the Dsync Groups are associated to. Required when configuring a team resource if a default team has not been set in the provider.
     */
    readonly teamId: string;
}
/**
 * Provides information about DSync groups for a team.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as std from "@pulumi/std";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = vercel.getDsyncGroups({
 *     teamId: "team_xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
 * });
 * const contractor = new vercel.AccessGroup("contractor", {
 *     teamId: "team_xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
 *     name: "contractor",
 *     description: "Access group for contractors",
 * });
 * const exampleTeamConfig = new vercel.TeamConfig("example", {
 *     id: "team_xxxxxxxxxxxxxxxxxxxxxxxxxxxx",
 *     saml: {
 *         enforced: true,
 *         roles: {
 *             [std.index.lookup({
 *                 map: exampleVercelDsyncGroups.map,
 *                 key: "admin",
 *             }).result]: {
 *                 role: "OWNER",
 *             },
 *             [std.index.lookup({
 *                 map: exampleVercelDsyncGroups.map,
 *                 key: "finance",
 *             }).result]: {
 *                 role: "BILLING",
 *             },
 *             [std.index.lookup({
 *                 map: exampleVercelDsyncGroups.map,
 *                 key: "contractor",
 *             }).result]: {
 *                 role: "CONTRIBUTOR",
 *                 accessGroupId: contractor.id,
 *             },
 *         },
 *     },
 * });
 * ```
 */
export function getDsyncGroupsOutput(args?: GetDsyncGroupsOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetDsyncGroupsResult> {
    args = args || {};
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vercel:index/getDsyncGroups:getDsyncGroups", {
        "teamId": args.teamId,
    }, opts);
}

/**
 * A collection of arguments for invoking getDsyncGroups.
 */
export interface GetDsyncGroupsOutputArgs {
    /**
     * The ID of the team the Dsync Groups are associated to. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}
