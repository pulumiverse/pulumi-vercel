// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Provides a Vercel Container Registry (VCR) Repository resource.
 *
 * A VCR Repository belongs to a Vercel Project and stores container images that can be
 * used by Vercel Functions and Vercel Sandbox. Images are pushed to and pulled from
 * `vcr.vercel.com/team-slug/project-slug/repository-name` using Docker-compatible tooling.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = new vercel.Project("example", {name: "example-project-with-vcr-repository"});
 * const exampleVcrRepository = new vercel.VcrRepository("example", {
 *     projectId: example.id,
 *     name: "my-repository",
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * If importing into a personal account, or with a team configured on
 * the provider, simply use the projectId and repository name.
 * - projectId can be found in the project `settings` tab in the Vercel UI.
 *
 * ```sh
 * $ pulumi import vercel:index/vcrRepository:VcrRepository example prj_xxxxxxxxxxxxxxxxxxxxxxxxxxxx/my-repository
 * ```
 *
 * Alternatively, you can import via the team_id, projectId and repository name.
 * - teamId can be found in the team `settings` tab in the Vercel UI.
 * - projectId can be found in the project `settings` tab in the Vercel UI.
 *
 * ```sh
 * $ pulumi import vercel:index/vcrRepository:VcrRepository example team_xxxxxxxxxxxxxxxxxxxxxxxx/prj_xxxxxxxxxxxxxxxxxxxxxxxxxxxx/my-repository
 * ```
 */
export class VcrRepository extends pulumi.CustomResource {
    /**
     * Get an existing VcrRepository resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: VcrRepositoryState, opts?: pulumi.CustomResourceOptions): VcrRepository {
        return new VcrRepository(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vercel:index/vcrRepository:VcrRepository';

    /**
     * Returns true if the given object is an instance of VcrRepository.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is VcrRepository {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === VcrRepository.__pulumiType;
    }

    /**
     * The name of the repository. Can contain lowercase letters, numbers, periods, underscores, and dashes, but cannot start or end with a period, underscore, or dash.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The ID of the existing Vercel Project the repository belongs to.
     */
    declare public readonly projectId: pulumi.Output<string>;
    /**
     * The ID of the team the repository should be created under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    declare public readonly teamId: pulumi.Output<string>;
    /**
     * The URL of the repository, composed of the owner slug, the project slug and the repository name (e.g. `vcr.vercel.com/team-slug/project-slug/repository-name`). Use it to push and pull images with Docker-compatible tooling.
     */
    declare public /*out*/ readonly url: pulumi.Output<string>;

    /**
     * Create a VcrRepository resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: VcrRepositoryArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: VcrRepositoryArgs | VcrRepositoryState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as VcrRepositoryState | undefined;
            resourceInputs["name"] = state?.name;
            resourceInputs["projectId"] = state?.projectId;
            resourceInputs["teamId"] = state?.teamId;
            resourceInputs["url"] = state?.url;
        } else {
            const args = argsOrState as VcrRepositoryArgs | undefined;
            if (args?.projectId === undefined && !opts.urn) {
                throw new Error("Missing required property 'projectId'");
            }
            resourceInputs["name"] = args?.name;
            resourceInputs["projectId"] = args?.projectId;
            resourceInputs["teamId"] = args?.teamId;
            resourceInputs["url"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(VcrRepository.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering VcrRepository resources.
 */
export interface VcrRepositoryState {
    /**
     * The name of the repository. Can contain lowercase letters, numbers, periods, underscores, and dashes, but cannot start or end with a period, underscore, or dash.
     */
    name?: pulumi.Input<string>;
    /**
     * The ID of the existing Vercel Project the repository belongs to.
     */
    projectId?: pulumi.Input<string>;
    /**
     * The ID of the team the repository should be created under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
    /**
     * The URL of the repository, composed of the owner slug, the project slug and the repository name (e.g. `vcr.vercel.com/team-slug/project-slug/repository-name`). Use it to push and pull images with Docker-compatible tooling.
     */
    url?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a VcrRepository resource.
 */
export interface VcrRepositoryArgs {
    /**
     * The name of the repository. Can contain lowercase letters, numbers, periods, underscores, and dashes, but cannot start or end with a period, underscore, or dash.
     */
    name?: pulumi.Input<string>;
    /**
     * The ID of the existing Vercel Project the repository belongs to.
     */
    projectId: pulumi.Input<string>;
    /**
     * The ID of the team the repository should be created under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}
