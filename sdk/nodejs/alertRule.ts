// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Creates a built-in Vercel alert rule using the Alerts v3 API. Built-in rules select anomaly triggers across a team scope. Notification channel links are managed separately from this resource.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = new vercel.Project("example", {name: "example-project"});
 * const checkoutErrors = new vercel.AlertRule("checkout_errors", {
 *     type: "built-in",
 *     name: "Checkout 5xx anomalies",
 *     ruleScope: {
 *         type: "include",
 *         projectIds: [example.id],
 *     },
 *     triggers: [{
 *         type: "error_anomaly",
 *         filter: "statusGroup:5xx AND route:/api/checkout",
 *     }],
 *     matchMinimumSeverityLevel: "high",
 *     notificationSettings: {
 *         enableTeamOwnerNotifications: true,
 *     },
 * });
 * ```
 *
 * ## Trigger filters
 *
 * Filter support depends on the trigger type.
 *
 * | Trigger type | Filter support | Allowed fields |
 * | --- | --- | --- |
 * | `errorAnomaly` | Required | `statusGroup`, `route` |
 * | `usageAnomaly` | Optional | `metrics` |
 *
 * ### Error anomaly filters
 *
 * An `errorAnomaly` filter must contain `statusGroup` with the value `4xx` or `5xx`. The optional `route` field supports exact values, wildcards, and regular expressions.
 *
 * Valid examples:
 *
 * `AND` can combine `statusGroup` and `route`, but `OR` and `NOT` cannot contain different fields. Consequently, `statusGroup:5xx OR route:/api/*` is invalid. A route-only filter is also invalid because `statusGroup` is required.
 *
 * ### Usage anomaly filters
 *
 * A `usageAnomaly` filter may contain only the `metrics` field with these values:
 *
 * - `fluidCpuDuration`
 * - `fluidDuration`
 * - `fastDataTransfer`
 * - `edgeRequests`
 * - `functionInvocations`
 *
 * Valid examples:
 *
 * Wildcards and regular expressions are not supported for metric names, so `metrics:edge*` is invalid.
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * If a default team is configured in the provider, use the alert_rule_id.
 *
 * ```sh
 * $ pulumi import vercel:index/alertRule:AlertRule example ar_xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
 * ```
 *
 * Otherwise, import via team_id/alert_rule_id.
 *
 * ```sh
 * $ pulumi import vercel:index/alertRule:AlertRule example team_xxxxxxxxxxxxxxxxxxxxxxxx/ar_xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
 * ```
 */
export class AlertRule extends pulumi.CustomResource {
    /**
     * Get an existing AlertRule resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AlertRuleState, opts?: pulumi.CustomResourceOptions): AlertRule {
        return new AlertRule(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vercel:index/alertRule:AlertRule';

    /**
     * Returns true if the given object is an instance of AlertRule.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AlertRule {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AlertRule.__pulumiType;
    }

    /**
     * Creation time as a Unix epoch timestamp in milliseconds.
     */
    declare public /*out*/ readonly createdAt: pulumi.Output<number>;
    /**
     * Whether this is the immutable team default rule. Default rules cannot be managed by this resource.
     */
    declare public /*out*/ readonly isDefault: pulumi.Output<boolean>;
    /**
     * The minimum severity matched by a built-in rule.
     */
    declare public readonly matchMinimumSeverityLevel: pulumi.Output<string>;
    /**
     * A human-readable name for the alert rule.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Notification delivery settings stored on the rule. Notification channel links are managed separately.
     */
    declare public readonly notificationSettings: pulumi.Output<outputs.AlertRuleNotificationSettings>;
    /**
     * The projects affected by the rule. Use `all`, `include`, or `exclude`.
     */
    declare public readonly ruleScope: pulumi.Output<outputs.AlertRuleRuleScope>;
    /**
     * The ID of the team that owns the alert rule. Required if a default team is not configured in the provider.
     */
    declare public readonly teamId: pulumi.Output<string>;
    /**
     * The API trigger selection mode. `all` and an empty `selected` set are response-only legacy states; omit `triggers` to preserve either state after import.
     */
    declare public /*out*/ readonly triggerMode: pulumi.Output<string>;
    /**
     * The built-in anomaly triggers enabled for a built-in rule. A nonempty set is required when creating a rule. Omit this attribute to preserve a response-only legacy trigger mode after import.
     */
    declare public readonly triggers: pulumi.Output<outputs.AlertRuleTrigger[]>;
    /**
     * The alert rule type. Currently only `built-in` is supported.
     */
    declare public readonly type: pulumi.Output<string>;
    /**
     * Last update time as a Unix epoch timestamp in milliseconds.
     */
    declare public /*out*/ readonly updatedAt: pulumi.Output<number>;

    /**
     * Create a AlertRule resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: AlertRuleArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AlertRuleArgs | AlertRuleState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AlertRuleState | undefined;
            resourceInputs["createdAt"] = state?.createdAt;
            resourceInputs["isDefault"] = state?.isDefault;
            resourceInputs["matchMinimumSeverityLevel"] = state?.matchMinimumSeverityLevel;
            resourceInputs["name"] = state?.name;
            resourceInputs["notificationSettings"] = state?.notificationSettings;
            resourceInputs["ruleScope"] = state?.ruleScope;
            resourceInputs["teamId"] = state?.teamId;
            resourceInputs["triggerMode"] = state?.triggerMode;
            resourceInputs["triggers"] = state?.triggers;
            resourceInputs["type"] = state?.type;
            resourceInputs["updatedAt"] = state?.updatedAt;
        } else {
            const args = argsOrState as AlertRuleArgs | undefined;
            if (args?.matchMinimumSeverityLevel === undefined && !opts.urn) {
                throw new Error("Missing required property 'matchMinimumSeverityLevel'");
            }
            if (args?.ruleScope === undefined && !opts.urn) {
                throw new Error("Missing required property 'ruleScope'");
            }
            if (args?.type === undefined && !opts.urn) {
                throw new Error("Missing required property 'type'");
            }
            resourceInputs["matchMinimumSeverityLevel"] = args?.matchMinimumSeverityLevel;
            resourceInputs["name"] = args?.name;
            resourceInputs["notificationSettings"] = args?.notificationSettings;
            resourceInputs["ruleScope"] = args?.ruleScope;
            resourceInputs["teamId"] = args?.teamId;
            resourceInputs["triggers"] = args?.triggers;
            resourceInputs["type"] = args?.type;
            resourceInputs["createdAt"] = undefined /*out*/;
            resourceInputs["isDefault"] = undefined /*out*/;
            resourceInputs["triggerMode"] = undefined /*out*/;
            resourceInputs["updatedAt"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AlertRule.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AlertRule resources.
 */
export interface AlertRuleState {
    /**
     * Creation time as a Unix epoch timestamp in milliseconds.
     */
    createdAt?: pulumi.Input<number>;
    /**
     * Whether this is the immutable team default rule. Default rules cannot be managed by this resource.
     */
    isDefault?: pulumi.Input<boolean>;
    /**
     * The minimum severity matched by a built-in rule.
     */
    matchMinimumSeverityLevel?: pulumi.Input<string>;
    /**
     * A human-readable name for the alert rule.
     */
    name?: pulumi.Input<string>;
    /**
     * Notification delivery settings stored on the rule. Notification channel links are managed separately.
     */
    notificationSettings?: pulumi.Input<inputs.AlertRuleNotificationSettings>;
    /**
     * The projects affected by the rule. Use `all`, `include`, or `exclude`.
     */
    ruleScope?: pulumi.Input<inputs.AlertRuleRuleScope>;
    /**
     * The ID of the team that owns the alert rule. Required if a default team is not configured in the provider.
     */
    teamId?: pulumi.Input<string>;
    /**
     * The API trigger selection mode. `all` and an empty `selected` set are response-only legacy states; omit `triggers` to preserve either state after import.
     */
    triggerMode?: pulumi.Input<string>;
    /**
     * The built-in anomaly triggers enabled for a built-in rule. A nonempty set is required when creating a rule. Omit this attribute to preserve a response-only legacy trigger mode after import.
     */
    triggers?: pulumi.Input<pulumi.Input<inputs.AlertRuleTrigger>[]>;
    /**
     * The alert rule type. Currently only `built-in` is supported.
     */
    type?: pulumi.Input<string>;
    /**
     * Last update time as a Unix epoch timestamp in milliseconds.
     */
    updatedAt?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a AlertRule resource.
 */
export interface AlertRuleArgs {
    /**
     * The minimum severity matched by a built-in rule.
     */
    matchMinimumSeverityLevel: pulumi.Input<string>;
    /**
     * A human-readable name for the alert rule.
     */
    name?: pulumi.Input<string>;
    /**
     * Notification delivery settings stored on the rule. Notification channel links are managed separately.
     */
    notificationSettings?: pulumi.Input<inputs.AlertRuleNotificationSettings>;
    /**
     * The projects affected by the rule. Use `all`, `include`, or `exclude`.
     */
    ruleScope: pulumi.Input<inputs.AlertRuleRuleScope>;
    /**
     * The ID of the team that owns the alert rule. Required if a default team is not configured in the provider.
     */
    teamId?: pulumi.Input<string>;
    /**
     * The built-in anomaly triggers enabled for a built-in rule. A nonempty set is required when creating a rule. Omit this attribute to preserve a response-only legacy trigger mode after import.
     */
    triggers?: pulumi.Input<pulumi.Input<inputs.AlertRuleTrigger>[]>;
    /**
     * The alert rule type. Currently only `built-in` is supported.
     */
    type: pulumi.Input<string>;
}
