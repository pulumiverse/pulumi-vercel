{
    "name": "@pulumi/vercel",
    "version": "${VERSION}",
    "scripts": {
        "build": "tsc",
        "install": "node scripts/install-pulumi-plugin.js resource vercel ${VERSION}"
    },
    "dependencies": {
        "@pulumi/pulumi": "^3.42.0"
    },
    "devDependencies": {
        "@types/node": "^14",
        "typescript": "^5.1.6"
    },
    "pulumi": {
        "resource": true,
        "name": "vercel"
    }
}
