// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides an OIDC Federation Policy resource.
 *
 * OIDC federation policies allow trusted external workloads to exchange an OIDC token for a short-lived Vercel access token. The client selects the Vercel CLI receiving access, while claims constrain the external workload identity.
 *
 * > This API currently supports first-party CLI clients enabled for the team, including the Vercel CLI and Turborepo CLI. The API token used by the provider must have permission to manage OIDC federation policies for the team.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const turborepoGithubActions = new vercel.OidcFederationPolicy("turborepo_github_actions", {
 *     name: "Turborepo for acme/widgets GitHub Workflows",
 *     client: "turborepo",
 *     issuerUrl: "https://token.actions.githubusercontent.com",
 *     claims: [
 *         {
 *             name: "aud",
 *             values: [{
 *                 value: "https://vercel.com/api/login/oauth/token",
 *             }],
 *         },
 *         {
 *             name: "repository",
 *             values: [{
 *                 value: "acme/widgets",
 *             }],
 *         },
 *     ],
 *     permissions: ["read-write:remote-cache"],
 *     resources: {
 *         projectIds: ["*"],
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * If importing with a team configured on the provider, use the policy ID.
 *
 * ```sh
 * $ pulumi import vercel:index/oidcFederationPolicy:OidcFederationPolicy example pol_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
 * ```
 *
 * Otherwise, use the team ID and policy ID.
 *
 * ```sh
 * $ pulumi import vercel:index/oidcFederationPolicy:OidcFederationPolicy example team_xxxxxxxxxxxxxxxxxxxxxxxx/pol_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
 * ```
 */
export class OidcFederationPolicy extends pulumi.CustomResource {
    /**
     * Get an existing OidcFederationPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OidcFederationPolicyState, opts?: pulumi.CustomResourceOptions): OidcFederationPolicy {
        return new OidcFederationPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vercel:index/oidcFederationPolicy:OidcFederationPolicy';

    /**
     * Returns true if the given object is an instance of OidcFederationPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is OidcFederationPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === OidcFederationPolicy.__pulumiType;
    }

    /**
     * OIDC claims that must all match for a token to be exchanged.
     */
    declare public readonly claims: pulumi.Output<outputs.OidcFederationPolicyClaim[]>;
    /**
     * The CLI whose access tokens this policy permits the workload to obtain. Valid values are `turborepo` and `vercel`.
     */
    declare public readonly client: pulumi.Output<string>;
    /**
     * Vercel CLI command IDs allowed for exchanged tokens. Only supported for the Vercel CLI client. Exactly one of `commands` or `permissions` must be configured.
     */
    declare public readonly commands: pulumi.Output<string[] | undefined>;
    /**
     * The HTTPS issuer URL in OIDC tokens recognized by this policy.
     */
    declare public readonly issuerUrl: pulumi.Output<string>;
    /**
     * A human-readable name describing the policy.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Permissions granted to exchanged tokens. For Turborepo remote caching, use `read:remote-cache` for read-only access or `read-write:remote-cache` for read and write access. Use `["*"]` only when the policy should inherit every permission authorized for the selected client. Exactly one of `permissions` or `commands` must be configured.
     */
    declare public readonly permissions: pulumi.Output<string[]>;
    /**
     * Optional project boundary for exchanged tokens. Omitting this block leaves access unbounded within the permissions authorized for the client.
     */
    declare public readonly resources: pulumi.Output<outputs.OidcFederationPolicyResources | undefined>;
    /**
     * The ID of the team the policy should exist under. Required when a default team has not been set in the provider.
     */
    declare public readonly teamId: pulumi.Output<string>;

    /**
     * Create a OidcFederationPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OidcFederationPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OidcFederationPolicyArgs | OidcFederationPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OidcFederationPolicyState | undefined;
            resourceInputs["claims"] = state?.claims;
            resourceInputs["client"] = state?.client;
            resourceInputs["commands"] = state?.commands;
            resourceInputs["issuerUrl"] = state?.issuerUrl;
            resourceInputs["name"] = state?.name;
            resourceInputs["permissions"] = state?.permissions;
            resourceInputs["resources"] = state?.resources;
            resourceInputs["teamId"] = state?.teamId;
        } else {
            const args = argsOrState as OidcFederationPolicyArgs | undefined;
            if (args?.claims === undefined && !opts.urn) {
                throw new Error("Missing required property 'claims'");
            }
            if (args?.client === undefined && !opts.urn) {
                throw new Error("Missing required property 'client'");
            }
            if (args?.issuerUrl === undefined && !opts.urn) {
                throw new Error("Missing required property 'issuerUrl'");
            }
            resourceInputs["claims"] = args?.claims;
            resourceInputs["client"] = args?.client;
            resourceInputs["commands"] = args?.commands;
            resourceInputs["issuerUrl"] = args?.issuerUrl;
            resourceInputs["name"] = args?.name;
            resourceInputs["permissions"] = args?.permissions;
            resourceInputs["resources"] = args?.resources;
            resourceInputs["teamId"] = args?.teamId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(OidcFederationPolicy.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering OidcFederationPolicy resources.
 */
export interface OidcFederationPolicyState {
    /**
     * OIDC claims that must all match for a token to be exchanged.
     */
    claims?: pulumi.Input<pulumi.Input<inputs.OidcFederationPolicyClaim>[]>;
    /**
     * The CLI whose access tokens this policy permits the workload to obtain. Valid values are `turborepo` and `vercel`.
     */
    client?: pulumi.Input<string>;
    /**
     * Vercel CLI command IDs allowed for exchanged tokens. Only supported for the Vercel CLI client. Exactly one of `commands` or `permissions` must be configured.
     */
    commands?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The HTTPS issuer URL in OIDC tokens recognized by this policy.
     */
    issuerUrl?: pulumi.Input<string>;
    /**
     * A human-readable name describing the policy.
     */
    name?: pulumi.Input<string>;
    /**
     * Permissions granted to exchanged tokens. For Turborepo remote caching, use `read:remote-cache` for read-only access or `read-write:remote-cache` for read and write access. Use `["*"]` only when the policy should inherit every permission authorized for the selected client. Exactly one of `permissions` or `commands` must be configured.
     */
    permissions?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Optional project boundary for exchanged tokens. Omitting this block leaves access unbounded within the permissions authorized for the client.
     */
    resources?: pulumi.Input<inputs.OidcFederationPolicyResources>;
    /**
     * The ID of the team the policy should exist under. Required when a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a OidcFederationPolicy resource.
 */
export interface OidcFederationPolicyArgs {
    /**
     * OIDC claims that must all match for a token to be exchanged.
     */
    claims: pulumi.Input<pulumi.Input<inputs.OidcFederationPolicyClaim>[]>;
    /**
     * The CLI whose access tokens this policy permits the workload to obtain. Valid values are `turborepo` and `vercel`.
     */
    client: pulumi.Input<string>;
    /**
     * Vercel CLI command IDs allowed for exchanged tokens. Only supported for the Vercel CLI client. Exactly one of `commands` or `permissions` must be configured.
     */
    commands?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * The HTTPS issuer URL in OIDC tokens recognized by this policy.
     */
    issuerUrl: pulumi.Input<string>;
    /**
     * A human-readable name describing the policy.
     */
    name?: pulumi.Input<string>;
    /**
     * Permissions granted to exchanged tokens. For Turborepo remote caching, use `read:remote-cache` for read-only access or `read-write:remote-cache` for read and write access. Use `["*"]` only when the policy should inherit every permission authorized for the selected client. Exactly one of `permissions` or `commands` must be configured.
     */
    permissions?: pulumi.Input<pulumi.Input<string>[]>;
    /**
     * Optional project boundary for exchanged tokens. Omitting this block leaves access unbounded within the permissions authorized for the client.
     */
    resources?: pulumi.Input<inputs.OidcFederationPolicyResources>;
    /**
     * The ID of the team the policy should exist under. Required when a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}
