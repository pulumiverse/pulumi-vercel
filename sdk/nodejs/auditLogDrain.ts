// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides an Audit Log Drain resource.
 *
 * Audit Log Drains forward team activity events to an HTTP endpoint or Amazon S3. They apply to the whole team and do not support project selection, filtering, or sampling.
 *
 * > Audit Log Drains are only available to Enterprise teams.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = new vercel.AuditLogDrain("example", {
 *     name: "security-audit-events",
 *     http: {
 *         endpoint: "https://siem.example.com/vercel",
 *         encoding: "ndjson",
 *         compression: "gzip",
 *         headers: {
 *             Authorization: "Bearer my-secret-token",
 *         },
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * If importing with a default team configured in the provider, simply use the
 * audit_log_drain_id.
 *
 * ```sh
 * $ pulumi import vercel:index/auditLogDrain:AuditLogDrain example drn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
 * ```
 *
 * Alternatively, import using team_id/audit_log_drain_id.
 *
 * ```sh
 * $ pulumi import vercel:index/auditLogDrain:AuditLogDrain example team_xxxxxxxxxxxxxxxxxxxxxxxx/drn_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
 * ```
 */
export class AuditLogDrain extends pulumi.CustomResource {
    /**
     * Get an existing AuditLogDrain resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: AuditLogDrainState, opts?: pulumi.CustomResourceOptions): AuditLogDrain {
        return new AuditLogDrain(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vercel:index/auditLogDrain:AuditLogDrain';

    /**
     * Returns true if the given object is an instance of AuditLogDrain.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is AuditLogDrain {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === AuditLogDrain.__pulumiType;
    }

    /**
     * Configuration for delivery to a custom HTTP endpoint. Exactly one of `http` or `s3` must be configured.
     */
    declare public readonly http: pulumi.Output<outputs.AuditLogDrainHttp | undefined>;
    /**
     * A human-readable name for the Audit Log Drain.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * Configuration for delivery to an Amazon S3 bucket. Exactly one of `http` or `s3` must be configured.
     */
    declare public readonly s3: pulumi.Output<outputs.AuditLogDrainS3 | undefined>;
    /**
     * The ID of the team the Audit Log Drain should exist under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    declare public readonly teamId: pulumi.Output<string>;

    /**
     * Create a AuditLogDrain resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: AuditLogDrainArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: AuditLogDrainArgs | AuditLogDrainState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as AuditLogDrainState | undefined;
            resourceInputs["http"] = state?.http;
            resourceInputs["name"] = state?.name;
            resourceInputs["s3"] = state?.s3;
            resourceInputs["teamId"] = state?.teamId;
        } else {
            const args = argsOrState as AuditLogDrainArgs | undefined;
            resourceInputs["http"] = args?.http;
            resourceInputs["name"] = args?.name;
            resourceInputs["s3"] = args?.s3;
            resourceInputs["teamId"] = args?.teamId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(AuditLogDrain.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering AuditLogDrain resources.
 */
export interface AuditLogDrainState {
    /**
     * Configuration for delivery to a custom HTTP endpoint. Exactly one of `http` or `s3` must be configured.
     */
    http?: pulumi.Input<inputs.AuditLogDrainHttp>;
    /**
     * A human-readable name for the Audit Log Drain.
     */
    name?: pulumi.Input<string>;
    /**
     * Configuration for delivery to an Amazon S3 bucket. Exactly one of `http` or `s3` must be configured.
     */
    s3?: pulumi.Input<inputs.AuditLogDrainS3>;
    /**
     * The ID of the team the Audit Log Drain should exist under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a AuditLogDrain resource.
 */
export interface AuditLogDrainArgs {
    /**
     * Configuration for delivery to a custom HTTP endpoint. Exactly one of `http` or `s3` must be configured.
     */
    http?: pulumi.Input<inputs.AuditLogDrainHttp>;
    /**
     * A human-readable name for the Audit Log Drain.
     */
    name?: pulumi.Input<string>;
    /**
     * Configuration for delivery to an Amazon S3 bucket. Exactly one of `http` or `s3` must be configured.
     */
    s3?: pulumi.Input<inputs.AuditLogDrainS3>;
    /**
     * The ID of the team the Audit Log Drain should exist under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}
