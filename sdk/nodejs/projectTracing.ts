// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Provides a Project Tracing resource.
 *
 * Project Tracing sends OpenTelemetry traces from a project's deployments to Vercel. Ordered sampling rules can limit which traces are retained.
 *
 * Deleting this resource disables tracing for the project.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vercel from "@pulumiverse/vercel";
 *
 * const example = new vercel.ProjectTracing("example", {
 *     projectId: exampleVercelProject.id,
 *     samplingRules: [{
 *         rate: 0.2,
 *         environment: "production",
 *         requestPath: "/api",
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * The `pulumi import` command can be used, for example:
 *
 * Project Tracing can be imported by specifying the project ID.
 *
 * ```sh
 * $ pulumi import vercel:index/projectTracing:ProjectTracing example prj_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
 * ```
 *
 * Alternatively, specify the team ID and project ID.
 *
 * ```sh
 * $ pulumi import vercel:index/projectTracing:ProjectTracing example team_xxxxxxxxxxxxxxxxxxxxxxxxxxxx/prj_xxxxxxxxxxxxxxxxxxxxxxxxxxxx
 * ```
 */
export class ProjectTracing extends pulumi.CustomResource {
    /**
     * Get an existing ProjectTracing resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ProjectTracingState, opts?: pulumi.CustomResourceOptions): ProjectTracing {
        return new ProjectTracing(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vercel:index/projectTracing:ProjectTracing';

    /**
     * Returns true if the given object is an instance of ProjectTracing.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ProjectTracing {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ProjectTracing.__pulumiType;
    }

    /**
     * The ID of the Project to configure tracing for.
     */
    declare public readonly projectId: pulumi.Output<string>;
    /**
     * Ordered head-sampling rules for traces. If omitted, all traces are retained.
     */
    declare public readonly samplingRules: pulumi.Output<outputs.ProjectTracingSamplingRule[] | undefined>;
    /**
     * The ID of the team the Project exists under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    declare public readonly teamId: pulumi.Output<string>;

    /**
     * Create a ProjectTracing resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: ProjectTracingArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ProjectTracingArgs | ProjectTracingState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ProjectTracingState | undefined;
            resourceInputs["projectId"] = state?.projectId;
            resourceInputs["samplingRules"] = state?.samplingRules;
            resourceInputs["teamId"] = state?.teamId;
        } else {
            const args = argsOrState as ProjectTracingArgs | undefined;
            if (args?.projectId === undefined && !opts.urn) {
                throw new Error("Missing required property 'projectId'");
            }
            resourceInputs["projectId"] = args?.projectId;
            resourceInputs["samplingRules"] = args?.samplingRules;
            resourceInputs["teamId"] = args?.teamId;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ProjectTracing.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering ProjectTracing resources.
 */
export interface ProjectTracingState {
    /**
     * The ID of the Project to configure tracing for.
     */
    projectId?: pulumi.Input<string>;
    /**
     * Ordered head-sampling rules for traces. If omitted, all traces are retained.
     */
    samplingRules?: pulumi.Input<pulumi.Input<inputs.ProjectTracingSamplingRule>[]>;
    /**
     * The ID of the team the Project exists under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ProjectTracing resource.
 */
export interface ProjectTracingArgs {
    /**
     * The ID of the Project to configure tracing for.
     */
    projectId: pulumi.Input<string>;
    /**
     * Ordered head-sampling rules for traces. If omitted, all traces are retained.
     */
    samplingRules?: pulumi.Input<pulumi.Input<inputs.ProjectTracingSamplingRule>[]>;
    /**
     * The ID of the team the Project exists under. Required when configuring a team resource if a default team has not been set in the provider.
     */
    teamId?: pulumi.Input<string>;
}
