# coding=utf-8
# *** WARNING: this file was generated by pulumi-language-python. ***
# *** Do not edit by hand unless you're certain you know what you are doing! ***

import builtins as _builtins
import warnings
import sys
import pulumi
import pulumi.runtime
from typing import Any, Mapping, Optional, Sequence, Union, overload
if sys.version_info >= (3, 11):
    from typing import NotRequired, TypedDict, TypeAlias
else:
    from typing_extensions import NotRequired, TypedDict, TypeAlias
from . import _utilities
from . import outputs
from ._inputs import *

__all__ = ['ProjectDeploymentCheckArgs', 'ProjectDeploymentCheck']

@pulumi.input_type
class ProjectDeploymentCheckArgs:
    def __init__(__self__, *,
                 project_id: pulumi.Input[_builtins.str],
                 requires: pulumi.Input[_builtins.str],
                 blocks: Optional[pulumi.Input[_builtins.str]] = None,
                 is_rerequestable: Optional[pulumi.Input[_builtins.bool]] = None,
                 name: Optional[pulumi.Input[_builtins.str]] = None,
                 source: Optional[pulumi.Input['ProjectDeploymentCheckSourceArgs']] = None,
                 targets: Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 team_id: Optional[pulumi.Input[_builtins.str]] = None,
                 timeout: Optional[pulumi.Input[_builtins.int]] = None):
        """
        The set of arguments for constructing a ProjectDeploymentCheck resource.

        :param pulumi.Input[_builtins.str] project_id: The ID or name of the Vercel project.
        :param pulumi.Input[_builtins.str] requires: The deployment stage required before the check runs: `build-ready`, `deployment-url`, or `none`. Changing this to `none` replaces the check because the API does not support that update.
        :param pulumi.Input[_builtins.str] blocks: The deployment stage blocked by the check. New checks currently support `deployment-alias` and `none`.
        :param pulumi.Input[_builtins.bool] is_rerequestable: Whether users can rerun the check. Defaults to false; must be false for a `git-provider` source.
        :param pulumi.Input[_builtins.str] name: The human-readable name of the Deployment Check.
        :param pulumi.Input['ProjectDeploymentCheckSourceArgs'] source: The system that supplies the Deployment Check. Required when creating a check; changing a configured source field replaces the check. Omit it when managing an imported check whose source is not writable through the API.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] targets: Deployment environments to which the check applies: `production`, `preview`, or a custom environment ID. Use `["all"]` for every environment; `all` cannot be combined with other targets. The API defaults to `["production"]`.
        :param pulumi.Input[_builtins.str] team_id: The ID of the Vercel team.
        :param pulumi.Input[_builtins.int] timeout: The timeout value supplied to check runners by the Checks API. When omitted, the API determines the value.
        """
        pulumi.set(__self__, "project_id", project_id)
        pulumi.set(__self__, "requires", requires)
        if blocks is not None:
            pulumi.set(__self__, "blocks", blocks)
        if is_rerequestable is not None:
            pulumi.set(__self__, "is_rerequestable", is_rerequestable)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if source is not None:
            pulumi.set(__self__, "source", source)
        if targets is not None:
            pulumi.set(__self__, "targets", targets)
        if team_id is not None:
            pulumi.set(__self__, "team_id", team_id)
        if timeout is not None:
            pulumi.set(__self__, "timeout", timeout)

    @_builtins.property
    @pulumi.getter(name="projectId")
    def project_id(self) -> pulumi.Input[_builtins.str]:
        """
        The ID or name of the Vercel project.
        """
        return pulumi.get(self, "project_id")

    @project_id.setter
    def project_id(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "project_id", value)

    @_builtins.property
    @pulumi.getter
    def requires(self) -> pulumi.Input[_builtins.str]:
        """
        The deployment stage required before the check runs: `build-ready`, `deployment-url`, or `none`. Changing this to `none` replaces the check because the API does not support that update.
        """
        return pulumi.get(self, "requires")

    @requires.setter
    def requires(self, value: pulumi.Input[_builtins.str]):
        pulumi.set(self, "requires", value)

    @_builtins.property
    @pulumi.getter
    def blocks(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The deployment stage blocked by the check. New checks currently support `deployment-alias` and `none`.
        """
        return pulumi.get(self, "blocks")

    @blocks.setter
    def blocks(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "blocks", value)

    @_builtins.property
    @pulumi.getter(name="isRerequestable")
    def is_rerequestable(self) -> Optional[pulumi.Input[_builtins.bool]]:
        """
        Whether users can rerun the check. Defaults to false; must be false for a `git-provider` source.
        """
        return pulumi.get(self, "is_rerequestable")

    @is_rerequestable.setter
    def is_rerequestable(self, value: Optional[pulumi.Input[_builtins.bool]]):
        pulumi.set(self, "is_rerequestable", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The human-readable name of the Deployment Check.
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter
    def source(self) -> Optional[pulumi.Input['ProjectDeploymentCheckSourceArgs']]:
        """
        The system that supplies the Deployment Check. Required when creating a check; changing a configured source field replaces the check. Omit it when managing an imported check whose source is not writable through the API.
        """
        return pulumi.get(self, "source")

    @source.setter
    def source(self, value: Optional[pulumi.Input['ProjectDeploymentCheckSourceArgs']]):
        pulumi.set(self, "source", value)

    @_builtins.property
    @pulumi.getter
    def targets(self) -> Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Deployment environments to which the check applies: `production`, `preview`, or a custom environment ID. Use `["all"]` for every environment; `all` cannot be combined with other targets. The API defaults to `["production"]`.
        """
        return pulumi.get(self, "targets")

    @targets.setter
    def targets(self, value: Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "targets", value)

    @_builtins.property
    @pulumi.getter(name="teamId")
    def team_id(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The ID of the Vercel team.
        """
        return pulumi.get(self, "team_id")

    @team_id.setter
    def team_id(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "team_id", value)

    @_builtins.property
    @pulumi.getter
    def timeout(self) -> Optional[pulumi.Input[_builtins.int]]:
        """
        The timeout value supplied to check runners by the Checks API. When omitted, the API determines the value.
        """
        return pulumi.get(self, "timeout")

    @timeout.setter
    def timeout(self, value: Optional[pulumi.Input[_builtins.int]]):
        pulumi.set(self, "timeout", value)


@pulumi.input_type
class _ProjectDeploymentCheckState:
    def __init__(__self__, *,
                 blocks: Optional[pulumi.Input[_builtins.str]] = None,
                 is_rerequestable: Optional[pulumi.Input[_builtins.bool]] = None,
                 name: Optional[pulumi.Input[_builtins.str]] = None,
                 project_id: Optional[pulumi.Input[_builtins.str]] = None,
                 requires: Optional[pulumi.Input[_builtins.str]] = None,
                 source: Optional[pulumi.Input['ProjectDeploymentCheckSourceArgs']] = None,
                 targets: Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 team_id: Optional[pulumi.Input[_builtins.str]] = None,
                 timeout: Optional[pulumi.Input[_builtins.int]] = None):
        """
        Input properties used for looking up and filtering ProjectDeploymentCheck resources.

        :param pulumi.Input[_builtins.str] blocks: The deployment stage blocked by the check. New checks currently support `deployment-alias` and `none`.
        :param pulumi.Input[_builtins.bool] is_rerequestable: Whether users can rerun the check. Defaults to false; must be false for a `git-provider` source.
        :param pulumi.Input[_builtins.str] name: The human-readable name of the Deployment Check.
        :param pulumi.Input[_builtins.str] project_id: The ID or name of the Vercel project.
        :param pulumi.Input[_builtins.str] requires: The deployment stage required before the check runs: `build-ready`, `deployment-url`, or `none`. Changing this to `none` replaces the check because the API does not support that update.
        :param pulumi.Input['ProjectDeploymentCheckSourceArgs'] source: The system that supplies the Deployment Check. Required when creating a check; changing a configured source field replaces the check. Omit it when managing an imported check whose source is not writable through the API.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] targets: Deployment environments to which the check applies: `production`, `preview`, or a custom environment ID. Use `["all"]` for every environment; `all` cannot be combined with other targets. The API defaults to `["production"]`.
        :param pulumi.Input[_builtins.str] team_id: The ID of the Vercel team.
        :param pulumi.Input[_builtins.int] timeout: The timeout value supplied to check runners by the Checks API. When omitted, the API determines the value.
        """
        if blocks is not None:
            pulumi.set(__self__, "blocks", blocks)
        if is_rerequestable is not None:
            pulumi.set(__self__, "is_rerequestable", is_rerequestable)
        if name is not None:
            pulumi.set(__self__, "name", name)
        if project_id is not None:
            pulumi.set(__self__, "project_id", project_id)
        if requires is not None:
            pulumi.set(__self__, "requires", requires)
        if source is not None:
            pulumi.set(__self__, "source", source)
        if targets is not None:
            pulumi.set(__self__, "targets", targets)
        if team_id is not None:
            pulumi.set(__self__, "team_id", team_id)
        if timeout is not None:
            pulumi.set(__self__, "timeout", timeout)

    @_builtins.property
    @pulumi.getter
    def blocks(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The deployment stage blocked by the check. New checks currently support `deployment-alias` and `none`.
        """
        return pulumi.get(self, "blocks")

    @blocks.setter
    def blocks(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "blocks", value)

    @_builtins.property
    @pulumi.getter(name="isRerequestable")
    def is_rerequestable(self) -> Optional[pulumi.Input[_builtins.bool]]:
        """
        Whether users can rerun the check. Defaults to false; must be false for a `git-provider` source.
        """
        return pulumi.get(self, "is_rerequestable")

    @is_rerequestable.setter
    def is_rerequestable(self, value: Optional[pulumi.Input[_builtins.bool]]):
        pulumi.set(self, "is_rerequestable", value)

    @_builtins.property
    @pulumi.getter
    def name(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The human-readable name of the Deployment Check.
        """
        return pulumi.get(self, "name")

    @name.setter
    def name(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "name", value)

    @_builtins.property
    @pulumi.getter(name="projectId")
    def project_id(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The ID or name of the Vercel project.
        """
        return pulumi.get(self, "project_id")

    @project_id.setter
    def project_id(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "project_id", value)

    @_builtins.property
    @pulumi.getter
    def requires(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The deployment stage required before the check runs: `build-ready`, `deployment-url`, or `none`. Changing this to `none` replaces the check because the API does not support that update.
        """
        return pulumi.get(self, "requires")

    @requires.setter
    def requires(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "requires", value)

    @_builtins.property
    @pulumi.getter
    def source(self) -> Optional[pulumi.Input['ProjectDeploymentCheckSourceArgs']]:
        """
        The system that supplies the Deployment Check. Required when creating a check; changing a configured source field replaces the check. Omit it when managing an imported check whose source is not writable through the API.
        """
        return pulumi.get(self, "source")

    @source.setter
    def source(self, value: Optional[pulumi.Input['ProjectDeploymentCheckSourceArgs']]):
        pulumi.set(self, "source", value)

    @_builtins.property
    @pulumi.getter
    def targets(self) -> Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]]:
        """
        Deployment environments to which the check applies: `production`, `preview`, or a custom environment ID. Use `["all"]` for every environment; `all` cannot be combined with other targets. The API defaults to `["production"]`.
        """
        return pulumi.get(self, "targets")

    @targets.setter
    def targets(self, value: Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]]):
        pulumi.set(self, "targets", value)

    @_builtins.property
    @pulumi.getter(name="teamId")
    def team_id(self) -> Optional[pulumi.Input[_builtins.str]]:
        """
        The ID of the Vercel team.
        """
        return pulumi.get(self, "team_id")

    @team_id.setter
    def team_id(self, value: Optional[pulumi.Input[_builtins.str]]):
        pulumi.set(self, "team_id", value)

    @_builtins.property
    @pulumi.getter
    def timeout(self) -> Optional[pulumi.Input[_builtins.int]]:
        """
        The timeout value supplied to check runners by the Checks API. When omitted, the API determines the value.
        """
        return pulumi.get(self, "timeout")

    @timeout.setter
    def timeout(self, value: Optional[pulumi.Input[_builtins.int]]):
        pulumi.set(self, "timeout", value)


@pulumi.type_token("vercel:index/projectDeploymentCheck:ProjectDeploymentCheck")
class ProjectDeploymentCheck(pulumi.CustomResource):
    @overload
    def __init__(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 blocks: Optional[pulumi.Input[_builtins.str]] = None,
                 is_rerequestable: Optional[pulumi.Input[_builtins.bool]] = None,
                 name: Optional[pulumi.Input[_builtins.str]] = None,
                 project_id: Optional[pulumi.Input[_builtins.str]] = None,
                 requires: Optional[pulumi.Input[_builtins.str]] = None,
                 source: Optional[pulumi.Input[Union['ProjectDeploymentCheckSourceArgs', 'ProjectDeploymentCheckSourceArgsDict']]] = None,
                 targets: Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 team_id: Optional[pulumi.Input[_builtins.str]] = None,
                 timeout: Optional[pulumi.Input[_builtins.int]] = None,
                 __props__=None):
        """
        Creates and manages a Deployment Check for a Vercel project.

        This resource manages a project-level check definition. It does not execute tests, create a GitHub workflow or webhook endpoint, or report check-run results. Configure the corresponding GitHub workflow, external runner, or integration separately.

        ## Example Usage

        ### GitHub and webhook checks

        Use a Vercel access token authorized for the project. GitHub checks currently support only `github`; `external_check_name` must match the check reported by the workflow. GitHub checks cannot be rerequestable through Vercel.

        ```python
        import pulumi
        import pulumiverse_vercel as vercel

        config = pulumi.Config()
        # GitHub repository connected to Vercel, in owner/repository format.
        github_repository = config.require("githubRepository")
        example = vercel.Project("example",
            name="deployment-checks-example",
            git_repository={
                "type": "github",
                "repo": github_repository,
            })
        # The external check name must match the check reported by your GitHub workflow.
        # This resource registers a gate; it does not create or run the workflow.
        e2e = vercel.ProjectDeploymentCheck("e2e",
            project_id=example.id,
            name="End-to-end tests",
            requires="deployment-url",
            blocks="deployment-alias",
            targets=["production"],
            is_rerequestable=False,
            source={
                "kind": "git-provider",
                "provider": "github",
                "external_check_name": "e2e",
            })
        # A webhook-backed check for an external test runner. The runner must handle
        # check events and report results through the Checks API separately.
        webhook = vercel.ProjectDeploymentCheck("webhook",
            project_id=example.id,
            name="External smoke tests",
            requires="deployment-url",
            blocks="none",
            targets=["preview"],
            is_rerequestable=True,
            source={
                "kind": "webhook",
            })
        ```

        ### Integration checks

        Integration checks require an integration installation token. The API identifies the integration and installation from that token. Use this token only with an `integration` source because it determines the returned source kind. The optional `external_resource_id` must belong to that installation.

        ```python
        import pulumi
        import pulumiverse_vercel as vercel

        config = pulumi.Config()
        # An integration installation token authorized for this project.
        integration_token = config.require("integrationToken")
        # The ID or name of an existing project accessible to the integration.
        project_id = config.require("projectId")
        # The team that owns the project.
        team_id = config.require("teamId")
        integration = vercel.ProjectDeploymentCheck("integration",
            project_id=project_id,
            name="Integration quality gate",
            requires="build-ready",
            blocks="deployment-alias",
            targets=["production"],
            source={
                "kind": "integration",
            })
        ```

        ## Choosing check behavior

        - `requires` selects when the check can run: `build-ready`, `deployment-url`, or `none`.
        - `blocks = "deployment-alias"` gates domain assignment; `blocks = "none"` makes the check non-blocking. Omission uses the API default, `deployment-alias`.
        - `targets` accepts `production`, `preview`, or custom environment IDs (not slugs). For a custom environment managed by Terraform, use `targets = [vercel_custom_environment.staging.id]`. Omission defaults to production. Use `["all"]` to select every environment; do not combine `all` with other targets or supply an empty set.
        - `is_rerequestable` defaults to false. Enable it only when your webhook runner or integration handles rerun requests.
        - Optional computed settings are populated from the API. Omitting a previously configured setting keeps its current value; it does not reset the setting to its creation default.

        Changing `project_id`, an explicitly configured `team_id`, or a configured source field replaces the check. Changing `requires` to `none` also replaces it because the API does not support that PATCH operation. Other supported settings update in place. Replacement deletes the old check and creates a new check with a new ID.

        ## Import

        Use `team_id/project_id/check_id`, or `project_id/check_id` when the provider supplies the team. Project names are supported; the imported identifier must match the `project_id` value in configuration to avoid replacement.

        With an explicit team:

        ```sh
        $ pulumi import vercel:index/projectDeploymentCheck:ProjectDeploymentCheck e2e team_xxx/prj_xxx/check_xxx
        ```

        With the team configured on the provider:

        ```sh
        $ pulumi import vercel:index/projectDeploymentCheck:ProjectDeploymentCheck e2e prj_xxx/check_xxx
        ```

        Project names are also supported. Use the same ID or name as project_id in config.

        ```sh
        $ pulumi import vercel:index/projectDeploymentCheck:ProjectDeploymentCheck e2e team_xxx/my-project/check_xxx
        ```

        For Terraform 1.5 and later:

        Match `name` and `requires` to the existing check before applying. Omit `source` to preserve an imported source, including Vercel-managed checks and Git-provider variants that this resource cannot create. Likewise, omit `blocks` to preserve an API value outside the configurable `deployment-alias` and `none` values. Inspect the imported state with `terraform state show` before configuring additional fields.

        A source is required for new checks. For an imported check with an omitted source, configuring a different source replaces it. Supply a writable source before changing `requires` to `none`; response-only sources cannot be recreated by this resource.


        :param str resource_name: The name of the resource.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] blocks: The deployment stage blocked by the check. New checks currently support `deployment-alias` and `none`.
        :param pulumi.Input[_builtins.bool] is_rerequestable: Whether users can rerun the check. Defaults to false; must be false for a `git-provider` source.
        :param pulumi.Input[_builtins.str] name: The human-readable name of the Deployment Check.
        :param pulumi.Input[_builtins.str] project_id: The ID or name of the Vercel project.
        :param pulumi.Input[_builtins.str] requires: The deployment stage required before the check runs: `build-ready`, `deployment-url`, or `none`. Changing this to `none` replaces the check because the API does not support that update.
        :param pulumi.Input[Union['ProjectDeploymentCheckSourceArgs', 'ProjectDeploymentCheckSourceArgsDict']] source: The system that supplies the Deployment Check. Required when creating a check; changing a configured source field replaces the check. Omit it when managing an imported check whose source is not writable through the API.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] targets: Deployment environments to which the check applies: `production`, `preview`, or a custom environment ID. Use `["all"]` for every environment; `all` cannot be combined with other targets. The API defaults to `["production"]`.
        :param pulumi.Input[_builtins.str] team_id: The ID of the Vercel team.
        :param pulumi.Input[_builtins.int] timeout: The timeout value supplied to check runners by the Checks API. When omitted, the API determines the value.
        """
        ...
    @overload
    def __init__(__self__,
                 resource_name: str,
                 args: ProjectDeploymentCheckArgs,
                 opts: Optional[pulumi.ResourceOptions] = None):
        """
        Creates and manages a Deployment Check for a Vercel project.

        This resource manages a project-level check definition. It does not execute tests, create a GitHub workflow or webhook endpoint, or report check-run results. Configure the corresponding GitHub workflow, external runner, or integration separately.

        ## Example Usage

        ### GitHub and webhook checks

        Use a Vercel access token authorized for the project. GitHub checks currently support only `github`; `external_check_name` must match the check reported by the workflow. GitHub checks cannot be rerequestable through Vercel.

        ```python
        import pulumi
        import pulumiverse_vercel as vercel

        config = pulumi.Config()
        # GitHub repository connected to Vercel, in owner/repository format.
        github_repository = config.require("githubRepository")
        example = vercel.Project("example",
            name="deployment-checks-example",
            git_repository={
                "type": "github",
                "repo": github_repository,
            })
        # The external check name must match the check reported by your GitHub workflow.
        # This resource registers a gate; it does not create or run the workflow.
        e2e = vercel.ProjectDeploymentCheck("e2e",
            project_id=example.id,
            name="End-to-end tests",
            requires="deployment-url",
            blocks="deployment-alias",
            targets=["production"],
            is_rerequestable=False,
            source={
                "kind": "git-provider",
                "provider": "github",
                "external_check_name": "e2e",
            })
        # A webhook-backed check for an external test runner. The runner must handle
        # check events and report results through the Checks API separately.
        webhook = vercel.ProjectDeploymentCheck("webhook",
            project_id=example.id,
            name="External smoke tests",
            requires="deployment-url",
            blocks="none",
            targets=["preview"],
            is_rerequestable=True,
            source={
                "kind": "webhook",
            })
        ```

        ### Integration checks

        Integration checks require an integration installation token. The API identifies the integration and installation from that token. Use this token only with an `integration` source because it determines the returned source kind. The optional `external_resource_id` must belong to that installation.

        ```python
        import pulumi
        import pulumiverse_vercel as vercel

        config = pulumi.Config()
        # An integration installation token authorized for this project.
        integration_token = config.require("integrationToken")
        # The ID or name of an existing project accessible to the integration.
        project_id = config.require("projectId")
        # The team that owns the project.
        team_id = config.require("teamId")
        integration = vercel.ProjectDeploymentCheck("integration",
            project_id=project_id,
            name="Integration quality gate",
            requires="build-ready",
            blocks="deployment-alias",
            targets=["production"],
            source={
                "kind": "integration",
            })
        ```

        ## Choosing check behavior

        - `requires` selects when the check can run: `build-ready`, `deployment-url`, or `none`.
        - `blocks = "deployment-alias"` gates domain assignment; `blocks = "none"` makes the check non-blocking. Omission uses the API default, `deployment-alias`.
        - `targets` accepts `production`, `preview`, or custom environment IDs (not slugs). For a custom environment managed by Terraform, use `targets = [vercel_custom_environment.staging.id]`. Omission defaults to production. Use `["all"]` to select every environment; do not combine `all` with other targets or supply an empty set.
        - `is_rerequestable` defaults to false. Enable it only when your webhook runner or integration handles rerun requests.
        - Optional computed settings are populated from the API. Omitting a previously configured setting keeps its current value; it does not reset the setting to its creation default.

        Changing `project_id`, an explicitly configured `team_id`, or a configured source field replaces the check. Changing `requires` to `none` also replaces it because the API does not support that PATCH operation. Other supported settings update in place. Replacement deletes the old check and creates a new check with a new ID.

        ## Import

        Use `team_id/project_id/check_id`, or `project_id/check_id` when the provider supplies the team. Project names are supported; the imported identifier must match the `project_id` value in configuration to avoid replacement.

        With an explicit team:

        ```sh
        $ pulumi import vercel:index/projectDeploymentCheck:ProjectDeploymentCheck e2e team_xxx/prj_xxx/check_xxx
        ```

        With the team configured on the provider:

        ```sh
        $ pulumi import vercel:index/projectDeploymentCheck:ProjectDeploymentCheck e2e prj_xxx/check_xxx
        ```

        Project names are also supported. Use the same ID or name as project_id in config.

        ```sh
        $ pulumi import vercel:index/projectDeploymentCheck:ProjectDeploymentCheck e2e team_xxx/my-project/check_xxx
        ```

        For Terraform 1.5 and later:

        Match `name` and `requires` to the existing check before applying. Omit `source` to preserve an imported source, including Vercel-managed checks and Git-provider variants that this resource cannot create. Likewise, omit `blocks` to preserve an API value outside the configurable `deployment-alias` and `none` values. Inspect the imported state with `terraform state show` before configuring additional fields.

        A source is required for new checks. For an imported check with an omitted source, configuring a different source replaces it. Supply a writable source before changing `requires` to `none`; response-only sources cannot be recreated by this resource.


        :param str resource_name: The name of the resource.
        :param ProjectDeploymentCheckArgs args: The arguments to use to populate this resource's properties.
        :param pulumi.ResourceOptions opts: Options for the resource.
        """
        ...
    def __init__(__self__, resource_name: str, *args, **kwargs):
        resource_args, opts = _utilities.get_resource_args_opts(ProjectDeploymentCheckArgs, pulumi.ResourceOptions, *args, **kwargs)
        if resource_args is not None:
            __self__._internal_init(resource_name, opts, **resource_args.__dict__)
        else:
            __self__._internal_init(resource_name, *args, **kwargs)

    def _internal_init(__self__,
                 resource_name: str,
                 opts: Optional[pulumi.ResourceOptions] = None,
                 blocks: Optional[pulumi.Input[_builtins.str]] = None,
                 is_rerequestable: Optional[pulumi.Input[_builtins.bool]] = None,
                 name: Optional[pulumi.Input[_builtins.str]] = None,
                 project_id: Optional[pulumi.Input[_builtins.str]] = None,
                 requires: Optional[pulumi.Input[_builtins.str]] = None,
                 source: Optional[pulumi.Input[Union['ProjectDeploymentCheckSourceArgs', 'ProjectDeploymentCheckSourceArgsDict']]] = None,
                 targets: Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]] = None,
                 team_id: Optional[pulumi.Input[_builtins.str]] = None,
                 timeout: Optional[pulumi.Input[_builtins.int]] = None,
                 __props__=None):
        opts = pulumi.ResourceOptions.merge(_utilities.get_resource_opts_defaults(), opts)
        if not isinstance(opts, pulumi.ResourceOptions):
            raise TypeError('Expected resource options to be a ResourceOptions instance')
        if opts.id is None:
            if __props__ is not None:
                raise TypeError('__props__ is only valid when passed in combination with a valid opts.id to get an existing resource')
            __props__ = ProjectDeploymentCheckArgs.__new__(ProjectDeploymentCheckArgs)

            __props__.__dict__["blocks"] = blocks
            __props__.__dict__["is_rerequestable"] = is_rerequestable
            __props__.__dict__["name"] = name
            if project_id is None and not opts.urn:
                raise TypeError("Missing required property 'project_id'")
            __props__.__dict__["project_id"] = project_id
            if requires is None and not opts.urn:
                raise TypeError("Missing required property 'requires'")
            __props__.__dict__["requires"] = requires
            __props__.__dict__["source"] = source
            __props__.__dict__["targets"] = targets
            __props__.__dict__["team_id"] = team_id
            __props__.__dict__["timeout"] = timeout
        super(ProjectDeploymentCheck, __self__).__init__(
            'vercel:index/projectDeploymentCheck:ProjectDeploymentCheck',
            resource_name,
            __props__,
            opts)

    @staticmethod
    def get(resource_name: str,
            id: pulumi.Input[str],
            opts: Optional[pulumi.ResourceOptions] = None,
            blocks: Optional[pulumi.Input[_builtins.str]] = None,
            is_rerequestable: Optional[pulumi.Input[_builtins.bool]] = None,
            name: Optional[pulumi.Input[_builtins.str]] = None,
            project_id: Optional[pulumi.Input[_builtins.str]] = None,
            requires: Optional[pulumi.Input[_builtins.str]] = None,
            source: Optional[pulumi.Input[Union['ProjectDeploymentCheckSourceArgs', 'ProjectDeploymentCheckSourceArgsDict']]] = None,
            targets: Optional[pulumi.Input[Sequence[pulumi.Input[_builtins.str]]]] = None,
            team_id: Optional[pulumi.Input[_builtins.str]] = None,
            timeout: Optional[pulumi.Input[_builtins.int]] = None) -> 'ProjectDeploymentCheck':
        """
        Get an existing ProjectDeploymentCheck resource's state with the given name, id, and optional extra
        properties used to qualify the lookup.

        :param str resource_name: The unique name of the resulting resource.
        :param pulumi.Input[str] id: The unique provider ID of the resource to lookup.
        :param pulumi.ResourceOptions opts: Options for the resource.
        :param pulumi.Input[_builtins.str] blocks: The deployment stage blocked by the check. New checks currently support `deployment-alias` and `none`.
        :param pulumi.Input[_builtins.bool] is_rerequestable: Whether users can rerun the check. Defaults to false; must be false for a `git-provider` source.
        :param pulumi.Input[_builtins.str] name: The human-readable name of the Deployment Check.
        :param pulumi.Input[_builtins.str] project_id: The ID or name of the Vercel project.
        :param pulumi.Input[_builtins.str] requires: The deployment stage required before the check runs: `build-ready`, `deployment-url`, or `none`. Changing this to `none` replaces the check because the API does not support that update.
        :param pulumi.Input[Union['ProjectDeploymentCheckSourceArgs', 'ProjectDeploymentCheckSourceArgsDict']] source: The system that supplies the Deployment Check. Required when creating a check; changing a configured source field replaces the check. Omit it when managing an imported check whose source is not writable through the API.
        :param pulumi.Input[Sequence[pulumi.Input[_builtins.str]]] targets: Deployment environments to which the check applies: `production`, `preview`, or a custom environment ID. Use `["all"]` for every environment; `all` cannot be combined with other targets. The API defaults to `["production"]`.
        :param pulumi.Input[_builtins.str] team_id: The ID of the Vercel team.
        :param pulumi.Input[_builtins.int] timeout: The timeout value supplied to check runners by the Checks API. When omitted, the API determines the value.
        """
        opts = pulumi.ResourceOptions.merge(opts, pulumi.ResourceOptions(id=id))

        __props__ = _ProjectDeploymentCheckState.__new__(_ProjectDeploymentCheckState)

        __props__.__dict__["blocks"] = blocks
        __props__.__dict__["is_rerequestable"] = is_rerequestable
        __props__.__dict__["name"] = name
        __props__.__dict__["project_id"] = project_id
        __props__.__dict__["requires"] = requires
        __props__.__dict__["source"] = source
        __props__.__dict__["targets"] = targets
        __props__.__dict__["team_id"] = team_id
        __props__.__dict__["timeout"] = timeout
        return ProjectDeploymentCheck(resource_name, opts=opts, __props__=__props__)

    @_builtins.property
    @pulumi.getter
    def blocks(self) -> pulumi.Output[_builtins.str]:
        """
        The deployment stage blocked by the check. New checks currently support `deployment-alias` and `none`.
        """
        return pulumi.get(self, "blocks")

    @_builtins.property
    @pulumi.getter(name="isRerequestable")
    def is_rerequestable(self) -> pulumi.Output[_builtins.bool]:
        """
        Whether users can rerun the check. Defaults to false; must be false for a `git-provider` source.
        """
        return pulumi.get(self, "is_rerequestable")

    @_builtins.property
    @pulumi.getter
    def name(self) -> pulumi.Output[_builtins.str]:
        """
        The human-readable name of the Deployment Check.
        """
        return pulumi.get(self, "name")

    @_builtins.property
    @pulumi.getter(name="projectId")
    def project_id(self) -> pulumi.Output[_builtins.str]:
        """
        The ID or name of the Vercel project.
        """
        return pulumi.get(self, "project_id")

    @_builtins.property
    @pulumi.getter
    def requires(self) -> pulumi.Output[_builtins.str]:
        """
        The deployment stage required before the check runs: `build-ready`, `deployment-url`, or `none`. Changing this to `none` replaces the check because the API does not support that update.
        """
        return pulumi.get(self, "requires")

    @_builtins.property
    @pulumi.getter
    def source(self) -> pulumi.Output['outputs.ProjectDeploymentCheckSource']:
        """
        The system that supplies the Deployment Check. Required when creating a check; changing a configured source field replaces the check. Omit it when managing an imported check whose source is not writable through the API.
        """
        return pulumi.get(self, "source")

    @_builtins.property
    @pulumi.getter
    def targets(self) -> pulumi.Output[Sequence[_builtins.str]]:
        """
        Deployment environments to which the check applies: `production`, `preview`, or a custom environment ID. Use `["all"]` for every environment; `all` cannot be combined with other targets. The API defaults to `["production"]`.
        """
        return pulumi.get(self, "targets")

    @_builtins.property
    @pulumi.getter(name="teamId")
    def team_id(self) -> pulumi.Output[_builtins.str]:
        """
        The ID of the Vercel team.
        """
        return pulumi.get(self, "team_id")

    @_builtins.property
    @pulumi.getter
    def timeout(self) -> pulumi.Output[_builtins.int]:
        """
        The timeout value supplied to check runners by the Checks API. When omitted, the API determines the value.
        """
        return pulumi.get(self, "timeout")

